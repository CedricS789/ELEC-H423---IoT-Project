# MAIN STEP TO DO

## 1. Key user generation with Morse code [Loic]

a. Debouncing button function  
b. Morse code translation  
c. PBKDF2 implementation   

## 2. ***AES encryption - DONE [Amaury]***

a. AES function  
b. Byte to String function for MQTT server data sending  

## 3. ***HMAC - DONE [Amaury]***

a. HMAC function  
b. Payload + HMAC sending to MQTT  

## 4. ***AES Decryption - DONE [Yuta]***

a. String to Binary translation function  
b. Decryption function  

## 5. ***HMAC Verification - DONE [Cedric]***

a. Verify if HMAC sent by first ESP32 is the same as the one generated by the second ESP32  
b. If not the same - discard the data  
c. If the same - Keep the data and send to the monitor (webserver)  

## 6. Port Knocking [Amaury]

a. Change port after each packet sending

## 7. Look for counter addition [Amaury]

# Initialization of the ESP32

a. Update the filesystem in platformIO when ESP is connected to load the Webserver in ESP32
